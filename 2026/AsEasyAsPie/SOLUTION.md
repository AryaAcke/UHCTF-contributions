1. XOR the plaintext & encrypted versions of the files together to get parts of the original XOR key
2. Upon inspecting the key, you can notice that the values are the decimal places of pi as string
3. Generate the full length XOR key by printing the digits of pi into a text file at least the length of the encrypted recipe
4. XOR this full length key with the encrypte raspberry pie recipe to find the flag in the plaintext result